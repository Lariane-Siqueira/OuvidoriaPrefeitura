let btnEnviar = document.getElementById("btn-enviar").addEventListener("click", enviar);
let inputs = document.getElementsByClassName("dados")[0]

function enviar() {
    if(inputs.hasAttribute("required")){
        
    }
}